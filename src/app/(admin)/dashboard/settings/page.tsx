import { PlaceholderPage } from '../_components/PlaceholderPage'

export const metadata = { title: "Configurações — Painel Sofia's" }

export default function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Sistema"
      title="Configurações"
      description="Configure os dados da pousada, número do WhatsApp, horários de check-in/out, políticas e integrações."
    />
  )
}
