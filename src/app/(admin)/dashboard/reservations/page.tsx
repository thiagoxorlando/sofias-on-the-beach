import { PlaceholderPage } from '../_components/PlaceholderPage'

export const metadata = { title: "Reservas — Painel Sofia's" }

export default function ReservationsPage() {
  return (
    <PlaceholderPage
      eyebrow="Gestão"
      title="Reservas"
      description="Visualize, confirme e gerencie todas as reservas da pousada. Inclui check-in, check-out e histórico completo."
    />
  )
}
